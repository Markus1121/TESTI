# testi/authentication.yaml
description: |
  ## Authentication
  This section describes authentication.

  - Use a **Bearer Token** for secure access.
  - Use the `/login` endpoint for token generation.
